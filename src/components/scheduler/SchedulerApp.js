/* global gapi */
import React, {Component} from 'react';
import '../../App.css';

class SchedulerApp extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <h1>Scheduler App</h1>
        );
    }
}

export default SchedulerApp;